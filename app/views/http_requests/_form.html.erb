<%= form_for(@http_request) do |f| %>
  <% if @http_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@http_request.errors.count, "error") %> prohibited this http_request from being saved:</h2>

      <ul>
      <% @http_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip %><br>
    <%= f.text_field :ip %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
